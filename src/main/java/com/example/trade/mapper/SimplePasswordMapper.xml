<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.trade.mapper.SimplePasswordMapper">
	<select id="checkSimPw" resultType="int">
	    SELECT COUNT(*)
	    FROM `user`
	    WHERE id = #{userId}
		AND simple_password IS NOT NULL
	</select>
	
	<select id="validatePassword" resultType="int">
		SELECT COUNT(*)
		FROM `user`
		WHERE id = #{userId}
		AND simple_password = #{simplePassword}
	</select>

	<update id="registerSimplePassword">
		UPDATE `user`
		SET simple_password = #{simplePassword}
		WHERE id = #{userId}
	</update>
</mapper>